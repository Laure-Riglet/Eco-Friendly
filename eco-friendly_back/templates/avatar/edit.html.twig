{% extends 'base.html.twig' %}

{% block title %}Modifier un avatar | Eco-Friendly{% endblock %}

{% block body %}

    <div class="container-fluid col-12 col-lg-8 mx-auto">
    <h1>
        Modifier l'avatar<br />
        "{{ avatar.name }}"
    </h1>
    <div>
        <div class="row mb-3">
            <div class="col-6">
                <span>Cr&eacute;&eacute; le {{ avatar.createdAt|date('d/m/Y') }}</span>
            </div>
            <div class="col-6 d-flex justify-content-end">
                Statut :&nbsp;&nbsp;
                {% include "_fragments/_category_active_tag.html.twig" with {status:avatar.isActive} %}
            </div>
        </div>
        {{ include('avatar/_form.html.twig', {'button_label': 'Valider'}) }}
        {{ include('avatar/_delete_form.html.twig') }}
    </div>
    <a href="{{ path('app_backoffice_avatars_list') }}">Retour à la liste</a>
    </div>

{% endblock %}