{% extends 'base.html.twig' %}

{% block title %}Modifier une cat&eacute;gorie | Eco-Friendly{% endblock %}

{% block body %}

    <div class="container-fluid col-12 col-lg-8 mx-auto">
    <h1>
        Modifier la cat&eacute;gorie<br />
        "{{ category.name }}"
    </h1>
    <div>
        <div class="row mb-3">
            <div class="col-6">
                <span>Cr&eacute;&eacute;e le {{ category.createdAt|date('d/m/Y') }}</span>
            </div>
            <div class="col-6 d-flex justify-content-end">
                Statut :&nbsp;&nbsp;
                {% include "_fragments/_category_active_tag.html.twig" with {status:category.isActive} %}
            </div>
        </div>
        {{ include('category/_form.html.twig', {'button_label': 'Valider'}) }}
        {{ include('category/_delete_form.html.twig') }}
    </div>
    <a href="{{ path('app_backoffice_categories_list') }}">Retour à la liste</a>
    </div>

{% endblock %}
