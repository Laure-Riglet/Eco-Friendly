{% extends 'base.html.twig' %}

{% block title %}Modifier un conseil | Eco-Friendly{% endblock %}

{% block body %}

    <div class="container-fluid col-12 col-lg-8 mx-auto">
    <h1>
        Modifier le conseil<br />
        "{{ advice.title }}"
    </h1>
    <div>
        <div class="row mb-3">
            <div class="col-6">
                <span>&Eacute;crit par {{ advice.contributor.nickname }}, </span>
                <span>le {{ advice.createdAt|date('d/m/Y') }}</span>
            </div>
            <div class="col-6 d-flex justify-content-end">
                Statut :&nbsp;&nbsp;
                {% include "_fragments/_content_status_tag.html.twig" with {status:advice.status} %}
            </div>
        </div>
        {{ include('advice/_form.html.twig', {'button_label': 'Valider'}) }}
        {{ include('advice/_delete_form.html.twig') }}
    </div>
    <a href="{{ path('app_backoffice_advices_list') }}">Retour à la liste</a>
    </div>

{% endblock %}
